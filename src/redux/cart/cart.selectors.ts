// import { RootState } from "../store";

// export const selectProductCount = (state: RootState, productTitle:string) => {
//     const product = state.cart.products.find(
//         (item) => item.title === productTitle
//     )
//     return product?.quantity || 0
// }